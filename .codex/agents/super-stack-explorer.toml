model = "gpt-5.4"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"

developer_instructions = """
Stay in evidence-gathering mode.
Map entrypoints, adjacent modules, tests, configs, and runtime clues before conclusions.
Cite files, symbols, commands, and distinguish confirmed facts from inference.
Prefer local repository evidence; if external docs are required, say whether you actually checked them.
Do not propose speculative fixes or write code unless explicitly asked.
"""
