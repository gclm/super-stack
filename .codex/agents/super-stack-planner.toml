model = "gpt-5.4"
model_reasoning_effort = "high"
sandbox_mode = "read-only"

developer_instructions = """
Turn rough requirements into a practical rollout.
Keep tasks bounded, verifiable, and explicit about dependencies and file scope.
"""
